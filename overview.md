# tutu 官网 — 代码结构说明（overview）

> 这份文件是给「未来的你 / 新接手的人」快速看懂本项目用的。
> 下次打开这个项目，先读这一份就够了。

---

## 一、项目是什么

**tutu** 是一个面向 **淘宝 / 拼多多卖家** 的「**商品图处理服务**」官网。

- 你的客户：电商卖家
- 你提供的服务：卖家发来产品「原图」，你把它处理成**能直接挂上商品链接的精修商品图**（去背、调光、换场景、尺寸合规等）
- 这个站的作用：**展示技能 + 接单**

## 二、技术栈（刻意保持简单）

| 项 | 选择 | 原因 |
|----|------|------|
| 前端 | 纯静态 HTML + CSS + 原生 JS | 零构建、打开即看、后续好接后端 |
| 后端 | 暂无 | 表单当前是「演示态」，只本地反馈，没接真实收单 |
| 部署 | 待定（GitHub Pages / CloudStudio 均可） | 静态站，随便放 |

## 三、文件清单

| 文件 | 作用 |
|------|------|
| `index.html` | 页面结构。每个区块都有中文注释标出（导航 / Hero / 服务 / 对比 / 流程 / 套餐 / 联系 / 页脚） |
| `styles.css` | 样式。墨黑 + 暖橙配色，响应式（手机自适应），含滑块与动画样式。顶部 `:root` 放了配色变量，改主题改这里 |
| `script.js` | 交互。移动端菜单切换、Before/After 拖动滑块、**上传图片预览（CSS 滤镜模拟精修）**、表单提交反馈 |
| `robots.txt` | SEO：告诉搜索引擎可以抓取全站，并指向 sitemap |
| `sitemap.xml` | SEO：站点地图，列出可收录页面，提交给搜索引擎用 |
| `overview.md` | **本说明文件** |

## 四、页面板块 → 对应代码位置

| 板块 | 在 index.html 的锚点 / class | 说明 |
|------|------------------------------|------|
| 导航栏 | `nav` | 锚点跳转各板块，移动端折叠 |
| Hero 主张 | `.hero` | 「专业商品图处理，让产品自己会卖货」+ 立即下单 CTA |
| 信任背书 | `.trust` | 服务店铺数 / 满意度 / 覆盖平台（纯展示） |
| 服务能力 | `#services` | 去背抠图 / 光影精修 / 场景合成 / 尺寸合规 / 批量处理 / 修改无忧 |
| 作品对比 | `#showcase` `.ba-slider` | 可拖动滑块的 Before/After；**支持「上传你的图看效果」**（访客传图后用 CSS 滤镜模拟精修前后） |
| 案例展示 | `#cases` `.gallery` | 6 个品类占位卡（运营后换真实作品） |
| 服务流程 | `#process` | 提交原图 → 沟通需求 → 精修交付 → 上架无忧 |
| 套餐价格 | `#pricing` | 体验版 / 标准版（标「最受欢迎」）/ 包月版 |
| 商家评价 | `#reviews` `.reviews` | 3 条示例评价（运营后换真实反馈） |
| 联系下单 | `#contact` | 微信占位 + 需求表单（演示态） |
| 常见问题 | `#faq` | FAQ 折叠问答 + FAQPage 结构化数据（利于搜索富摘要） |
| 页脚 | `footer` | 版权与备案占位 |

> SEO 相关（上线后用于被搜索引擎收录）：`index.html` 含 `canonical`、Open Graph、JSON-LD（Service + FAQPage 结构化数据）；另有 `robots.txt` 与 `sitemap.xml`。

## 五、怎么本地预览

```bash
# 方式一：直接双击 index.html 用浏览器打开

# 方式二（推荐，避免个别浏览器本地文件限制）：
cd tutu
python3 -m http.server 8000
# 然后浏览器打开 http://localhost:8000
```

## 六、待你替换的「占位内容」（重要）

目前很多内容是占位，上线前需替换：

1. **Before/After 案例图** —— 用真实处理案例替换（`#showcase` 区块；也可让客户用「上传你的图看效果」直接预览）
2. **案例展示图** —— 用真实作品替换 `#cases` 区块的 6 个占位卡
3. **商家评价** —— 换成真实客户反馈（`#reviews` 区块）
4. **微信二维码 / 微信号** —— `#contact` 区块，当前是占位
5. **套餐价格** —— `#pricing` 区块，按你实际报价改
6. **表单接后端** —— 当前只本地弹提示，未真实收单（需接服务/接口）

## 七、怎么部署上线

- **GitHub Pages**：仓库 Settings → Pages → Source 选 `main` 分支 → 保存，几分钟后 `https://gushan-park.github.io/tutu/` 即可访问
- **CloudStudio**：把仓库导入，静态托管
- 注意：如果以后用到国内可访问域名，国内站点需 **ICP 备案**

## 八、怎么改内容（给未来的自己）

- 改文案 → 编辑 `index.html` 对应区块（都有注释）
- 改配色/风格 → 编辑 `styles.css` 顶部 `:root` 变量
- 改交互 → 编辑 `script.js`
- 想加后端/真实下单 → 在 `script.js` 的表单提交处接 fetch 到你的接口

---

*最后更新：2026-08-29（v2：新增信任条 / 上传预览交互 / 案例展示 / 商家评价，并修正板块 class 命名）*
